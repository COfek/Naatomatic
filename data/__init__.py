"""Data layer — generation and services (repositories).

See PROJECT_STRUCTURE.md. `generation/` populates the DB; `services/` is the only
place that writes it (transactional repositories used by tools and the Validator).
"""
