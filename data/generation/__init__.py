"""Data generation — how a fresh database gets populated.

Planned:
  generate_data.py  random/bulk data via Faker (migrates from scripts/)
  seed.py           small curated, known fixtures for tests/demos
  fixtures/         optional static fixture data
"""
