"""Constraint engine — the single source of truth for hard rules (HC-*).

Both the data generator and (later) the live agent's Validator node call these
functions, so a rule is defined exactly once.
"""
