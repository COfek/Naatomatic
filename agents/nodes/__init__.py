"""Reusable graph nodes.

Planned:
  worker.py     ReAct reasoning step (decides next tool call or final answer)
  validator.py  Validator node — calls rules/constraints.py before any commit
  presenter.py  formats the final user-facing answer (HE/EN)
"""
