"""Per-pillar worker configuration (system prompts + tool subsets).

Planned: network.py, logistics.py, guard_duty.py, adhoc.py.
Each wires its pillar's tools (from tools/) and prompt into a Worker node.
"""
