"""Shared tool infrastructure — contracts and registry.

  base.py      ToolContext, ToolOutput, require_role, tool_spec generation
  registry.py  aggregates all domain tools into one namespace; dispatch
"""
